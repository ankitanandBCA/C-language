#include<stdio.h>
#include<string.h>
int main()
{
   char str[]="ankit ";
   int len;
   len=strlen(str);
   printf("%d",len);
   int len1=strlen("hello world is the program for bigning");
   printf("\n%d",len1);
}